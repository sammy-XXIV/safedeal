[package]
name = "safedeal-client"
version = "0.1.0"
edition = "2021"

[dependencies]
sails-rs = { version = "1.0", features = ["build"] }

[build-dependencies]
sails-rs = { version = "1.0", features = ["build"] }
