[package]
name = "safedeal-app"
version = "0.1.0"
edition = "2021"

[dependencies]
sails-rs = "1.0"

[dev-dependencies]
sails-rs = { version = "1.0", features = ["gtest"] }

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = true
